@page "/RealEstates"
@inherits BaseRealEstate

<link href="css/RealEstateList.css" rel="stylesheet" />

<h3>Market Place</h3>

@if (realEstates.Count() == 0)
{
    <div class="spinner"></div>
}
else
{
    var counter = 0;

    @foreach (var estate in realEstates)
    {
        
        <div class="card flex-row mb-3" style="width: 90vw; height: 60vh;">
                
                <img src="https://i.imgur.com/HyXS72k.jpg" style="height: 100%; width:60%;" alt="Picture of house for sale and or rent">
                
                <div class="flex-column" style="height: 100%; width:40%;">

                    @*<div>
                        <h5 class="mt-3 ml-3 mb-0">@estate.Title</h5>
                        <span class="ml-3 city">Varberg, @BuyersOptions(estate)</span>
                    </div>

                    <div class="flex-row mt-5 ml-3">
                        <span>@string.Format("{0:N}", estate.SellingPrice) kr</span>
                        <span class="ml-5">@squareMeters[counter] m²</span>
                        <span class="ml-5">@rooms[counter] rum</span>
                    </div>
                    
                    <div>
                        <p class="ml-3">@estate.Description</p>
                    </div>*@
                    @*<a href="@($"RealEstate/{estate.Id}")" class="btn btn-primary">View</a>*@
                </div>
        </div>
        counter++;
        if (counter == 5)
            counter = 0;
    }
}

@code
{
    int[] squareMeters = new int[] { 60, 80, 75, 100, 48 };
    int[] rooms = new int[] { 3, 5, 3, 4, 1 };
}

